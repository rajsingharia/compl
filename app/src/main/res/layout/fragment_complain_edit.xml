<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:app="http://schemas.android.com/apk/res-auto"
android:layout_height="fill_parent"
android:layout_width="match_parent"
android:fillViewport="true"
xmlns:tools="http://schemas.android.com/tools"
xmlns:android="http://schemas.android.com/apk/res/android"
tools:context=".view.fragments.complainers.ComplainEditFragment">

<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="20dp"
    android:gravity="center"
    android:padding="10dp">


    <EditText
        android:id="@+id/complain_edit_title"
        android:paddingStart="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
        android:background="@drawable/round_corner_grey_bgd"
        android:hint="Title"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/complain_edit_description"
        android:layout_width="match_parent"
        android:paddingStart="20dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/complain_edit_title"
        android:layout_marginTop="30dp"
        android:background="@drawable/round_corner_grey_bgd"
        android:hint="Description"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/complain_edit_phone_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/complain_edit_description"
        android:paddingStart="20dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/round_corner_grey_bgd"
        android:hint="Phone Number"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:textSize="16sp" />
    <EditText
        android:id="@+id/complain_edit_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/complain_edit_phone_number"
        android:paddingStart="20dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/round_corner_grey_bgd"
        android:hint="Address"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:textSize="16sp" />


    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/complain_edit_save_data_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/complain_edit_address"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:background="@drawable/login_signup_btn_bgd"
        android:text="Make Changes"
        android:padding="10dp"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold" />



</RelativeLayout>
</ScrollView>